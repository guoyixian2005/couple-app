# LeanCloud 配置指南

这个应用使用 LeanCloud 提供实时数据库和用户管理功能。按照以下步骤设置 LeanCloud：

## 什么是 LeanCloud？

LeanCloud 是国内领先的 BaaS（Backend as a Service）平台，提供：
- 📱 用户认证
- 📊 实时数据库  
- 💾 文件存储
- 🔔 消息推送
- 📈 数据分析

**免费额度**：非常适合个人项目和小型应用

## 步骤 1：注册 LeanCloud 账号

1. 访问 [LeanCloud 官网](https://leancloud.cn/)
2. 点击右上角 **"注册"**
3. 填写注册信息（推荐使用邮箱注册）
4. 验证邮箱并登录

## 步骤 2：创建应用

1. 登录后进入 [LeanCloud 控制台](https://console.leancloud.cn/)
2. 点击 **"创建应用"**
3. 选择 **"开发版"**（免费）
4. 输入应用名称（例如："couple-app"）
5. 选择应用类型：**"JavaScript"**
6. 点击 **"创建"**

## 步骤 3：获取配置信息

1. 在应用列表中找到刚创建的应用，点击进入
2. 点击左侧菜单的 **"设置"** → **"应用凭证"**
3. 你会看到以下信息：
   - **App ID**：应用唯一标识
   - **App Key**：应用密钥
   - **服务器地址**：通常是 `https://api.leancloud.cn`

## 步骤 4：创建数据表

### 方法一：通过控制台创建（推荐）

1. 点击左侧菜单的 **"数据存储"** → **"结构化数据"**
2. 点击 **"创建 Class"**，创建以下数据表：

#### 创建 Message 表（消息表）
- Class 名称：`Message`
- 添加字段：
  - `content`（String）：消息内容
  - `senderId`（String）：发送者ID
  - `receiverId`（String）：接收者ID
  - `read`（Boolean）：是否已读

#### 创建 PairRequest 表（配对请求表）
- Class 名称：`PairRequest`
- 添加字段：
  - `requesterId`（String）：发起者ID
  - `requesterEmail`（String）：发起者邮箱
  - `targetEmail`（String）：目标邮箱
  - `status`（String）：请求状态（pending/accepted/rejected）

### 方法二：自动创建（代码运行时自动创建）

- 应用会在首次运行时自动创建所需的数据表
- 但推荐手动创建以确保结构正确

## 步骤 5：配置环境变量

1. 在项目根目录创建 `.env.local` 文件：

```bash
cp .env.local.example .env.local
```

2. 编辑 `.env.local` 文件，填入你的配置：

```env
NEXT_PUBLIC_LEANCLOUD_APP_ID=你的App_ID
NEXT_PUBLIC_LEANCLOUD_APP_KEY=你的App_Key
NEXT_PUBLIC_LEANCLOUD_SERVER_URL=https://api.leancloud.cn
```

## 步骤 6：设置数据表权限（安全）

在 **"数据存储"** → **"结构化数据"** → **"其他"** → **"权限管理"** 中设置：

### Message 表权限：
- **创建**：所有用户可创建
- **读取**：仅消息发送者或接收者可读
- **更新**：仅消息发送者可更新
- **删除**：仅消息发送者可删除

### PairRequest 表权限：
- **创建**：所有用户可创建
- **读取**：仅请求发起者或目标用户可读
- **更新**：仅请求发起者或目标用户可更新
- **删除**：仅请求发起者可删除

### _User 表权限（用户表）：
- 保持默认设置即可

## 步骤 7：启用实时功能（可选但推荐）

1. 点击左侧菜单的 **"消息"** → **"实时通信"**
2. 点击 **"启用实时通信"**
3. 这将支持消息的实时推送功能

## 完成！

配置完成后，你可以：

1. 运行 `npm run dev` 启动开发服务器
2. 在浏览器中打开 `http://localhost:3000`
3. 注册两个账号来测试情侣配对功能

## 测试流程

### 测试账号A（发起者）：
1. 注册账号（例如：alice@example.com）
2. 点击 **"发送配对请求"**
3. 输入账号B的邮箱

### 测试账号B（接受者）：
1. 在另一个浏览器或无痕窗口注册（例如：bob@example.com）
2. 会看到配对请求通知
3. 点击 **"接受"**
4. 开始聊天！

## 故障排除

### 问题：无法连接到 LeanCloud
- 检查 `.env.local` 文件是否正确配置
- 确认 App ID 和 App Key 是否正确
- 检查网络连接

### 问题：消息无法发送
- 确保 Message 表已创建
- 检查浏览器控制台的错误信息
- 确认数据表权限设置正确

### 问题：实时更新不工作
- 确认实时通信功能已启用
- 检查网络连接
- 查看控制台是否有连接错误

### 问题：配对请求无法发送
- 确认目标邮箱已注册
- 检查 PairRequest 表是否创建
- 确认数据表权限设置正确

## 数据管理

### 查看数据：
- 在控制台的 **"数据存储"** 中可以查看所有存储的数据
- 支持查询、过滤、排序等操作

### 清理数据：
- 可以手动删除测试数据
- 或在控制台批量删除

### 备份数据：
- LeanCloud 自动备份数据
- 也可以导出数据进行备份

## 免费额度说明

LeanCloud 开发版免费额度：
- **API 请求**：每月 5 万次
- **数据存储**：500MB
- **实时连接**：10 个并发连接
- **文件存储**：1GB

对于情侣应用来说，这些额度完全够用！

## 下一步

配置完成后，你可以开始添加更多功能：
- 学习/上班打卡
- 虚拟宠物
- 文件分享
- 视频通话
- 等等...